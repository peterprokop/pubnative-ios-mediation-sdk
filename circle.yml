machine:
    timezone:
        Europe/Berlin
    xcode:
        version: "7.1"

test:
    override:
        - xcodebuild
            -workspace mediation.xcworkspace
            -sdk iphonesimulator
            -scheme mediation
            clean test | xcpretty -c

#---------------------- TODO:
#   master:
#        branch: master
#        commands:
#            - Create library, tag and release on GitHub
